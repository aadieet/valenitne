<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>A Special Premiere 💕</title>

<style>
body {
    margin: 0;
    font-family: 'Georgia', serif;
    background: radial-gradient(circle at center, #ff4da6 0%, #b30059 100%);
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
    overflow: hidden;
    color: white;
}

/* Movie spotlight effect */
body::before {
    content: "";
    position: absolute;
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, rgba(255,255,255,0.2) 0%, transparent 70%);
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
}

/* Card */
.card {
    background: rgba(0,0,0,0.6);
    padding: 50px;
    border-radius: 40px;
    box-shadow: 0 20px 40px rgba(0,0,0,0.4);
    width: 90%;
    max-width: 450px;
    position: relative;
    z-index: 2;
}

h1 {
    margin-bottom: 10px;
    font-size: 26px;
}

.subtitle {
    font-size: 14px;
    opacity: 0.8;
    margin-bottom: 25px;
}

/* Buttons */
button {
    padding: 14px 28px;
    font-size: 16px;
    border: none;
    border-radius: 40px;
    cursor: pointer;
    margin: 10px;
    transition: 0.3s;
}

.yes {
    background-color: #ff0066;
    color: white;
}

.yes:hover {
    transform: scale(1.1);
}

.no {
    background-color: white;
    color: #ff0066;
}

/* Floating hearts */
.heart {
    position: absolute;
    font-size: 20px;
    animation: floatUp 4s linear infinite;
}

@keyframes floatUp {
    0% {
        transform: translateY(0) scale(1);
        opacity: 1;
    }
    100% {
        transform: translateY(-800px) scale(1.5);
        opacity: 0;
    }
}

</style>
</head>

<body>

<div class="card">
    <h1>🎬 Tonight's Feature Presentation 🎬</h1>
    <div class="subtitle">Starring: Aadit & Harhsni</div>
    <h2>Harhsni, will you be my Valentine? 💕</h2>

    <button class="yes" onclick="yesClicked()">Yes 💖</button>
    <button class="no" onclick="noClicked()">No 😢</button>

    <div id="response" style="margin-top:20px;"></div>
</div>

<script>
let noCount = 0;

function yesClicked() {
    document.getElementById("response").innerHTML = `
    <h2>And I was wondering… ❤️</h2>
    <iframe width="300" height="170"
    src="https://www.youtube.com/embed/QwZzP7gQX6k?autoplay=1"
    frameborder="0"
    allow="autoplay; encrypted-media"
    allowfullscreen></iframe>`;
    
    createHearts();
}

function noClicked() {
    noCount++;

    if (noCount === 1) {
        document.getElementById("response").innerHTML =
        "<h3>Aadit will miss you 💔</h3>";
    } 
    else if (noCount === 2) {
        document.getElementById("response").innerHTML =
        "<h3>He will be sad 😔</h3>";
    }
    else if (noCount === 3) {
        document.getElementById("response").innerHTML =
        "<h3>Aadit loves you ❤️</h3>";
        createHearts();
    }
    else {
        document.getElementById("response").innerHTML =
        "<h3>Okay okay… last chance 😭💕</h3>";
    }
}

function createHearts() {
    for (let i = 0; i < 20; i++) {
        let heart = document.createElement("div");
        heart.className = "heart";
        heart.innerHTML = "❤️";
        heart.style.left = Math.random() * 100 + "vw";
        heart.style.top = "100vh";
        heart.style.animationDuration = (Math.random() * 3 + 2) + "s";
        document.body.appendChild(heart);

        setTimeout(() => {
            heart.remove();
        }, 4000);
    }
}
</script>

</body>
</html>
